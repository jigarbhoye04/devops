apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: helios
  labels:
    app: helios
    component: analytics-service
spec:
  selector:
    app: helios
    component: analytics-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
